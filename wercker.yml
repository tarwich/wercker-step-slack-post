box: wercker/default
build:
  steps:
    - validate-wercker-step
    - script:
        name: dump env
        code: set
  after-steps:
    - script:
        name: run myself as a test
        code: . ./run.sh
